# FL7.2 Fast Lane PAPER BUY — Implementation Plan

Base: sealed FL7.1 merged main `c870d90d30168c236736b8690c63ee082b0ade07`, CI `33742529186` four-gate green.

LIVE remains disabled.

## Objective

Add the smallest truthful bridge from a sealed Fast Lane `BUY` assessment to preserved PAPER risk, execution, and ledger accounting while enforcing exact quantity, landing latency, capacity, max entry price, and max total entry spend.

## Commit / TDD sequence

### 1. Design

Committed as `docs/superpowers/specs/2026-09-03-fl7-2-paper-buy-design.md`.

### 2. Plan

This file.

### 3. RED public contract

Add `python/tests/test_fast_paper_buy.py` before production symbols exist.

The RED test imports and exercises the intended public API:

Risk:
- `FastEntryRiskRequest`
- `FastEntryRiskAssessment`
- `assess_fast_entry_risk`

Fast PAPER:
- `FAST_PAPER_BUY_VERSION`
- `FastPaperBuyApproval`
- `FastPaperBuyQuote`
- `FastPaperBuyOutcome`
- `FastPaperBuyResult`
- `FastPaperBuyError`
- `execute_fast_paper_buy`

Expected RED: Python collection/import failure only. Repository safety, Rust, and ARM64 should remain green.

### 4. Fast Lane risk request

Add `python/src/shreks_brain/risk/fast_entry.py`.

Use existing:
- `RiskPolicy`
- `RiskContext`
- `RiskState`
- `RiskFinding`
- `RiskReasonCode`
- `TradeIntent`
- `TradeSide`
- `RuntimeMode`

Do not construct legacy `TradeDecision`.

Add only the required new reason code to `risk/models.py`:
- `REQUESTED_NOTIONAL_EXCEEDS_RISK_CAP`

Add exports in `risk/__init__.py`.

Fast risk must:
- require PAPER mode;
- retain original decision timestamp on `TradeIntent`;
- evaluate current risk context at `evaluated_at_unix_ms`;
- enforce action/state compatibility versions;
- duplicate the preserved guardrail behavior without changing legacy `assess_entry_risk`;
- approve the exact requested notional only when it is at/below the preserved risk-sized upper bound;
- reject rather than resize when the requested notional is too large;
- use an explicit `not-applicable:fast-lane` score-policy compatibility marker;
- derive a deterministic action idempotency key.

### 5. FL7.2 BUY models

Add `python/src/shreks_brain/fast_paper/buy_models.py`.

Define validated immutable models for:
- Rust-derived BUY approval/economics boundary;
- native-quote landing evidence;
- outcomes/result/error.

Approval must require the embedded FL7.1 assessment action to be `BUY`.

Executable quote state must require native reference/execution prices, quoted/available base capacity, and quote-to-USD evidence.

### 6. FL7.2 BUY engine

Add `python/src/shreks_brain/fast_paper/buy.py`.

Execution order:
1. validate clocks and identity;
2. apply landing latency / quote-window semantics;
3. native max-price gate;
4. exact-base-capacity gate;
5. convert exact landing quantity to USD notional;
6. run Fast Lane exact-notional risk;
7. short-circuit already-terminal ledger keys;
8. adapt native quote to existing `PaperQuote`;
9. call existing pure `execute_paper_intent`;
10. require full fill quantity parity;
11. check actual total quote spend against approved max total spend before ledger mutation;
12. call existing `apply_paper_execution` only when allowed;
13. return complete evidence.

No provider I/O, sleeps, wall-clock reads, DB reads, or LIVE calls.

### 7. Public export

Update only `python/src/shreks_brain/fast_paper/__init__.py` for FL7.2 symbols.

### 8. Candidate verification

Audit PR scope before trusting CI.

Expected changed files:
- FL7.2 design doc;
- FL7.2 plan doc;
- `risk/models.py` additive reason;
- `risk/fast_entry.py` new;
- `risk/__init__.py` exports;
- `fast_paper/buy_models.py` new;
- `fast_paper/buy.py` new;
- `fast_paper/__init__.py` exports;
- `python/tests/test_fast_paper_buy.py` new.

No existing PAPER engine, ledger, legacy paper-loop, provider, storage, Rust strategy, signer, submission, deployment, or LIVE authority file should change.

Run canonical four gates on candidate head.

### 9. Clean history

After candidate green, collapse only post-RED implementation authoring commits into one clean implementation commit. Preserve:

`design -> plan -> RED -> implementation`

Move branch ref to the byte-identical clean tree and require fresh exact-head four-gate CI.

### 10. Guarded merge + seal

Update PR proof body, mark ready, and merge only with `expected_head_sha` equal to the fresh verified clean head.

Require a fresh push-triggered merged-main four-gate CI on the returned merge SHA.

Only then mark FL7.2 SEALED and begin FL7.3.

## Test matrix

The final Python test must cover:

1. Fast risk approves exact requested notional without resizing.
2. Requested notional above risk cap is rejected.
3. Kill switch and degraded health reject.
4. Action-assessment/state-version mismatches reject.
5. Compatibility intent keeps original decision timestamp and truthful Fast Lane metadata.
6. Evaluation before assumed landing latency defers without risk/execution/ledger mutation.
7. Missing/too-early quote defers while still inside the quote window.
8. Quote above Rust max entry price aborts.
9. Quoted or available base quantity below intended size aborts.
10. Successful BUY opens an exact-quantity PAPER position through existing fill + ledger APIs.
11. Raw price under max but total simulated entry spend above approved maximum aborts before ledger mutation.
12. Risk rejection leaves ledger byte-equivalent/equal.
13. Filled terminal replay returns already processed and does not duplicate exposure.
14. Failed-after-submission network cost is booked by the preserved ledger when complete quote evidence exists.
15. Non-BUY assessment, mint/quote identity mismatch, future quote, invalid rates, and malformed numerics fail closed.
16. Existing full Python suite remains green.

## Completion claim boundary

FL7.2 may be called SEALED only after both exact-head and fresh merged-main four-gate CI are green.

Do not claim profitability or LIVE readiness from FL7.2.